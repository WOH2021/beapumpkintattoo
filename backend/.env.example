PORT=3001
# Use the Supabase "Connection string (URI)" from Project → Settings → Database
DATABASE_URL=postgresql://user:password@db.host:5432/database
# Optional: restrict CORS to your front-end origin
CORS_ORIGIN=https://beapumpkintattoo.com
# JWT secret for authentication (use a long random string in production)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
